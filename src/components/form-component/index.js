import styled, { css } from "styled-components";


export const Error = styled.span`
    color:var(--danger-color);
    font-size: 0.8rem;
    font-weight: 500;
    min-height: 0.8rem;
    display: block;
`
